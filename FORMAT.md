# مرجع صيغة YTD

> مرجع تقني لملفات `.ytd` في GTA V و FiveM، يشرح ما تفعله ملفات `core/`.
> كُتب ليبقى الكود نظيفًا بلا تعليقات مع بقاء المعرفة متاحة عند الحاجة.
>
> **Wun Studio** — تطوير Athlawi

---

## المحتويات

- [نظرة عامة](#نظرة-عامة)
- [الطبقة الأولى: حاوية RSC7](#الطبقة-الأولى-حاوية-rsc7)
- [الطبقة الثانية: قاموس التكستشرات](#الطبقة-الثانية-قاموس-التكستشرات)
- [الطبقة الثالثة: بيانات البكسل](#الطبقة-الثالثة-بيانات-البكسل)
- [الترقيع في المكان](#الترقيع-في-المكان)
- [خريطة الملفات](#خريطة-الملفات)

---

## نظرة عامة

ملف `.ytd` ثلاث طبقات متداخلة:

```
ملف .ytd
└── حاوية RSC7            ترويسة 16 بايت + جسم مضغوط بـ DEFLATE
    ├── مقطع النظام        الهياكل والأسماء وجداول المؤشرات
    │   └── pgDictionary   قاموس فيه مصفوفة مؤشرات إلى grcTexturePC
    │       └── grcTexturePC × N   وصف كل تكستشر
    └── مقطع الجرافيكس     بيانات البكسل الخام لكل التكستشرات
```

---

## الطبقة الأولى: حاوية RSC7

كل ملف موارد في اللعبة (`.ytd` و `.ydr` و `.yft` وغيرها) حاوية RSC7.

### الترويسة

| الإزاحة | الحجم | الحقل |
|---|---|---|
| `0x00` | 4 | التوقيع `RSC7` = `0x37435352` بترتيب little-endian |
| `0x04` | 4 | الإصدار — `13` لملفات `.ytd` |
| `0x08` | 4 | `systemFlags` — يرمّز حجم مقطع النظام |
| `0x0C` | 4 | `graphicsFlags` — يرمّز حجم مقطع الجرافيكس |
| `0x10` | .. | تيار DEFLATE خام **بلا ترويسة zlib** (‏`wbits = -15`) |

### فكّ حقول الحجم

الحقلان ليسا حجمًا مباشرًا بل حقلان بتّيان يرمّزان **عدد الصفحات** بأحجام
متضاعفة. البتات الأربع الدنيا أُسّ حجم الصفحة الأساسي، وبقية المجموعات
عدّادات لصفحات بأحجام مضاعفة:

| البتات | العرض | المضاعف |
|---|---|---|
| `27` | 1 بت | ×1 |
| `26` | 1 بت | ×2 |
| `25` | 1 بت | ×4 |
| `24` | 1 بت | ×8 |
| `17–23` | 7 بتات | ×16 |
| `11–16` | 6 بتات | ×32 |
| `7–10` | 4 بتات | ×64 |
| `5–6` | 2 بت | ×128 |
| `4` | 1 بت | ×256 |
| `0–3` | 4 بتات | أُسّ حجم الصفحة الأساسي |

```
حجم الصفحة الأساسي = 0x200 << ss
الحجم الكلي = حجم الصفحة الأساسي × مجموع كل العدّادات مضروبة بمضاعفاتها
```

هذا التخطيط يطابق `RpfResourceFileEntry.GetSizeFromFlags` في CodeWalker.

### المقطعان

فكّ ضغط الجسم يعطي **بالضبط** `systemSize + graphicsSize` بايت:

```
[ مقطع النظام ][ مقطع الجرافيكس ]
```

> إن جاء الناتج أقصر من المتوقع فذلك طبيعي: الحزّام كثيرًا ما يحذف صفحات
> الأصفار الأخيرة. تُكمَّل بأصفار.

### المؤشرات

المؤشرات داخل المورد **64 بت**، والنيبل الأعلى يحدد المقطع:

| النمط | المقطع | المحتوى |
|---|---|---|
| `0x5.......` | النظام | الهياكل، الأسماء، جداول المؤشرات |
| `0x6.......` | الجرافيكس | بيانات البكسل الخام |

البتات الـ 28 السفلى إزاحة بايت مباشرة داخل المقطع، فترجمة العنوان تصير
عملية واحدة بعد فصل المقطعين.

---

## الطبقة الثانية: قاموس التكستشرات

مقطع النظام يبدأ بـ `pgDictionary<grcTexture>` بحجم `0x40` بايت عند الإزاحة صفر:

| الإزاحة | النوع | الحقل |
|---|---|---|
| `0x00` | u32 | `FileVFT` |
| `0x04` | u32 | `FileUnknown` = 1 |
| `0x08` | u64 | مؤشر `FilePagesInfo` |
| `0x20` | u64 | مؤشر مصفوفة hashes الأسماء (‏u32 Jenkins، مرتّبة) |
| `0x28` | u16 ×2 | عدد الـ hashes، ثم السعة |
| `0x30` | u64 | **مؤشر مصفوفة التكستشرات** (مصفوفة مؤشرات u64) |
| `0x38` | u16 ×2 | **عدد التكستشرات**، ثم السعة |

### هيكل التكستشر الواحد

`grcTexturePC` بحجم `0x90` بايت، وأول `0x50` منه هو `TextureBase`:

| الإزاحة | النوع | الحقل |
|---|---|---|
| `0x28` | u64 | مؤشر الاسم — نص ASCII منتهٍ بصفر |
| `0x50` | u16 | العرض |
| `0x52` | u16 | الارتفاع |
| `0x54` | u16 | العمق |
| `0x56` | u16 | `Stride` |
| `0x58` | u32 | الصيغة — كود `D3DFORMAT` |
| `0x5D` | u8 | عدد مستويات الـ mip |
| `0x70` | u64 | **مؤشر بيانات البكسل** في مقطع الجرافيكس |

> **تنبيه:** موضع مؤشر البكسلات يختلف بين إصدارات اللعبة والأدوات. الترتيب
> المعتمد `0x70`، وتُجرَّب بعده `0x60` و `0x68` و `0x78`، ثم يُمسح ذيل الهيكل
> بحثًا عن أي qword يشير إلى مقطع الجرافيكس.

### التحليل الاحتياطي

إن فشل التخطيط القياسي يُبحث داخل الهيكل عن كود صيغة معروف، ثم تُعاد بقية
الحقول **نسبيًا** حوله، لأن الترتيب النسبي ثابت حتى لو انزاحت الإزاحات
المطلقة:

```
الصيغة − 8  = العرض       الصيغة − 6 = الارتفاع
الصيغة − 4  = العمق        الصيغة − 2 = Stride
الصيغة + 5  = المستويات    الصيغة + 8 = مؤشر البيانات
```

---

## الطبقة الثالثة: بيانات البكسل

الأسطح مخزّنة **خامًا**: بلا ترويسة DDS وبلا جدول إزاحات. مستويات الـ mip
ملصوقة ببعضها بالتتابع:

```
مستوى 0 (w × h)، مستوى 1 (w/2 × h/2)، … مستوى N
```

اللعبة تعيد حساب التخطيط من (العرض، الارتفاع، الصيغة، عدد المستويات)، فأي
أداة تحرير ملزَمة بحساب الشيء نفسه بالبايت.

### حجم المستوى الواحد

**الصيغ المضغوطة كتليًا** — كتل 4×4:

```
عدد الكتل أفقيًا = ceil(العرض / 4)
عدد الكتل رأسيًا = ceil(الارتفاع / 4)
الحجم = الكتل الأفقية × الكتل الرأسية × بايتات الكتلة
```

**الصيغ غير المضغوطة:**

```
الحجم = ceil(العرض × الارتفاع × البتات لكل بكسل / 8)
```

### جدول الصيغ

| الصيغة | الكود | بايت/كتلة أو بت/بكسل | ملاحظات |
|---|---|---|---|
| DXT1 (BC1) | `'DXT1'` | 8 بايت | ألفا بمستوى واحد (punch-through) |
| DXT3 (BC2) | `'DXT3'` | 16 بايت | ألفا صريحة 4 بت |
| DXT5 (BC3) | `'DXT5'` | 16 بايت | الأكثر شيوعًا في GTA V |
| ATI1 (BC4) | `'ATI1'` | 8 بايت | قناة واحدة |
| ATI2 (BC5) | `'ATI2'` | 16 بايت | قناتان — خرائط النتوء |
| BC7 | `'BC7 '` | 16 بايت | جودة عالية، ألفا كاملة |
| A8R8G8B8 | 21 | 32 بت | بلا فقد |
| X8R8G8B8 | 22 | 32 بت | الألفا معتمة دائمًا |
| A1R5G5B5 | 25 | 16 بت | ألفا بمستوى واحد |
| A8 | 28 | 8 بت | ألفا فقط |
| A8B8G8R8 | 32 | 32 بت | بلا فقد |
| L8 | 50 | 8 بت | إضاءة |

> **BC7:** فكّ الترميز يمرّ عبر Pillow بترويسة DDS اصطناعية من نوع DX10
> (‏`DXGI_FORMAT_BC7_UNORM` = 98). أما الترميز فيستخدم **الوضع 6** حصرًا:
> الوضع الوحيد ذو المجموعة الواحدة والألفا الكاملة الذي يمكن ترميزه بلا بحث
> مكلف عن التقسيمات.

---

## الترقيع في المكان

هذا القرار الهندسي المحوري في المشروع.

عند الحفظ **لا يُعاد بناء القاموس**. تُكتب بايتات البكسل الجديدة فوق القديمة
مباشرة داخل المقطع المفكوك، ثم يُعاد ضغط المقطعين كما هما وتُعاد أعلام
الترويسة حرفيًا.

### المكسب

- كل اسم و hash ومؤشر يبقى كما كتبته Rockstar
- كل تكستشر لم يُلمس يبقى **مطابقًا بايت-ببايت**
- تخطيط الصفحات وأعلام الترويسة تبقى صالحة بلا حساب

### الثمن

التكستشر المعدَّل لا بد أن يُعاد ترميزه إلى **نفس عدد البايتات بالضبط**: نفس
الأبعاد، نفس الصيغة، نفس عدد المستويات.

### الحمايات الثلاث

1. **فحص الأبعاد** — ترفض الكتابة إن اختلف مقاس الصورة عن الأصلي
2. **فحص الحجم المرمَّز** — ترفض إن لم يساوِ الحجم الأصلي بالبايت
3. **حساب المدى** — تُحسب المسافة حتى بداية التكستشر التالي في مقطع
   الجرافيكس، فلا يُكتب أبدًا فوق تكستشر مجاور

وقبل الكتابة تُعاد قراءة البايتات الناتجة كاملة للتأكد أنها ما زالت قابلة
للتحليل.

### استرجاع البكسلات

الحفظ يعدّل المورد المحمّل في الذاكرة، فلو حُفظ الملف ثم تراجع المستخدم عن
تعديل ثم حُفظ مرة أخرى، بقي التعديل القديم في الناتج. لذلك تُحفظ نسخة من
بايتات كل تكستشر **قبل أول ترقيع له**، وتُستعاد في بداية كل حفظة، فيعكس
الملف المكتوب التعديلات الحالية فقط.

### إعادة توليد الـ mips

عند تعديل تكستشر تُعاد سلسلة الـ mips **كاملة** لا المستوى صفر فقط، لأن
اللعبة تعاين المستويات الأدنى عن بُعد؛ ولو بقيت القديمة لظهر التعديل يختفي
كلما ابتعدت الكاميرا.

---

## خريطة الملفات

| الملف | المسؤولية |
|---|---|
| `core/rsc7.py` | الحاوية: الترويسة، فكّ الأعلام، الضغط، ترجمة المؤشرات |
| `core/ytd_handler.py` | القاموس: التحليل، حساب المدى، الترقيع، الاسترجاع، الفحص |
| `core/texture_handler.py` | الـ codecs: BC1–BC7 والصيغ غير المضغوطة، سلاسل الـ mips |
| `core/export_handler.py` | تنسيق الحفظ، تصدير PNG و DDS، استيراد الصور |
| `core/adjust.py` | التعديلات اللونية، دلو التعبئة، التدرّج |

---

## حدود معروفة

- **RSC8** (‏Red Dead Redemption 2) يُكتشف ويُرفض برسالة واضحة
- قواميس **Gen9** تستخدم حجم هيكل مختلفًا وغير مدعومة
- الملفات داخل `.rpf` لا بد أن تُستخرج أولًا بـ OpenIV أو CodeWalker
- الضغط الكتلي فاقد، فدورات فتح وتعديل وحفظ متكررة تُدهور الجودة تدريجيًا
